/**
 * KIP-37 API
 * ## Introduction The KIP-37 API helps Blockchain app (BApp) developers to easily deploy smart contracts and send tokens of the [KIP-37 Multi Token Standard](https://kips.klaytn.com/KIPs/kip-37).  You can use the default contract managing account (`deployer`) and `alias`.    You can also manage the contracts and tokens created on the klaytn network using the caver SDK, using contract address and the [Wallet API](https://refs.klaytnapi.com/ko/wallet/latest) account.    ## Fee Payer Options  KAS KIP-37 supports four scenarios for paying transactin fees:      **1. Using only KAS Global FeePayer Account**   Sends all transactions using the KAS global FeePayer Account.       ``` {     \"options\": {       \"enableGlobalFeePayer\": true     }     } ```    <br />    **2. Using User FeePayer account**   Sends all transactions using the KAS User FeePayer Account.      ``` {   \"options\": {     \"enableGlobalFeePayer\": false,     \"userFeePayer\": {       \"krn\": \"krn:1001:wallet:20bab367-141b-439a-8b4c-ae8788b86316:feepayer-pool:default\",       \"address\": \"0xd6905b98E4Ba43a24E842d2b66c1410173791cab\"     }   } } ```    <br />  **3. Using both KAS Global FeePayer Account + User FeePayer Account**   Uses User FeePayer Account as default. When the balance runs out, KAS Global FeePayer Account will be used.     ``` {   \"options\": {     \"enableGlobalFeePayer\": true,     \"userFeePayer\": {       \"krn\": \"krn:1001:wallet:20bab367-141b-439a-8b4c-ae8788b86316:feepayer-pool:default\",       \"address\": \"0xd6905b98E4Ba43a24E842d2b66c1410173791cab\"     }   } } ```    <br />  **4. Not using FeePayer Account**   Sends a transaction via normal means where the sender pays the transaction fee.       ``` {   \"options\": {     \"enableGlobalFeePayer\": false   } } ```
 *
 * OpenAPI spec version: 1.0
 *
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 * Do not edit the class manually.
 *
 */

const ApiClient = require('../../ApiClient')

/**
 * The TransferKip37TokenRequest model module.
 * @class TransferKip37TokenRequest
 * @version 1.0
 */
class TransferKip37TokenRequest {
    /**
     * Constructs a new <code>TransferKip37TokenRequest</code>.
     * @alias TransferKip37TokenRequest
     * @class
     * @param sender {String} The Klaytn account address to send the tokens
     * @param owner {String} The Klaytn account address that owns the tokens
     * @param to {String} The Klaytn account address to receive the tokens
     * @param ids {Array.<String>} Array of IDs of the tokens to send
     * @param amounts {Array.<String>} Array of the tokens to send (in hex.)
     */

    constructor(sender, owner, to, ids, amounts) {
        this.sender = sender
        this.owner = owner
        this.to = to
        this.ids = ids
        this.amounts = amounts
    }

    /**
     * Constructs a <code>TransferKip37TokenRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {TransferKip37TokenRequest} obj Optional instance to populate.
     * @return {TransferKip37TokenRequest} The populated <code>TransferKip37TokenRequest</code> instance.
     * @memberof TransferKip37TokenRequest
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new TransferKip37TokenRequest()

            if (data.hasOwnProperty('sender')) {
                obj.sender = ApiClient.convertToType(data.sender, 'String')
            }
            if (data.hasOwnProperty('owner')) {
                obj.owner = ApiClient.convertToType(data.owner, 'String')
            }
            if (data.hasOwnProperty('to')) {
                obj.to = ApiClient.convertToType(data.to, 'String')
            }
            if (data.hasOwnProperty('ids')) {
                obj.ids = ApiClient.convertToType(data.ids, ['String'])
            }
            if (data.hasOwnProperty('amounts')) {
                obj.amounts = ApiClient.convertToType(data.amounts, ['String'])
            }
        }
        return obj
    }
}

/**
 * The Klaytn account address to send the tokens
 * @type {String}
 * @memberof TransferKip37TokenRequest
 */
TransferKip37TokenRequest.prototype.sender = undefined
/**
 * The Klaytn account address that owns the tokens
 * @type {String}
 * @memberof TransferKip37TokenRequest
 */
TransferKip37TokenRequest.prototype.owner = undefined
/**
 * The Klaytn account address to receive the tokens
 * @type {String}
 * @memberof TransferKip37TokenRequest
 */
TransferKip37TokenRequest.prototype.to = undefined
/**
 * Array of IDs of the tokens to send
 * @type {Array.<String>}
 * @memberof TransferKip37TokenRequest
 */
TransferKip37TokenRequest.prototype.ids = undefined
/**
 * Array of the tokens to send (in hex.)
 * @type {Array.<String>}
 * @memberof TransferKip37TokenRequest
 */
TransferKip37TokenRequest.prototype.amounts = undefined

module.exports = TransferKip37TokenRequest
