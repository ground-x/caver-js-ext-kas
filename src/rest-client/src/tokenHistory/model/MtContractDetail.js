/*
 * Token History API
 * # Introduction  Token History API allows users to search for information and transfer records on KLAY, FT (KIP-7, Labeled ERC-20), and NFT (KIP-17, Labeled ERC-721) tokens. You can use Token History API to check the records of a specific EOA transferring KLAY, retrieve NFT information, or other purposes.  For more details on Token History API, refer to our [tutorial](https://klaytn.com).  For any questions regarding this document or KAS, visit [the developer forum](https://forum.klaytn.com/).
 *
 * OpenAPI spec version: 1.0
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 2.4.18
 *
 * Do not edit the class manually.
 *
 */

;(function(root, factory) {
    if (typeof define === 'function' && define.amd) {
        // AMD. Register as an anonymous module.
        define(['../../ApiClient'], factory)
    } else if (typeof module === 'object' && module.exports) {
        // CommonJS-like environments that support module.exports, like Node.
        module.exports = factory(require('../../ApiClient'))
    } else {
        // Browser globals (root is window)
        if (!root.TokenHistoryApi) {
            root.TokenHistoryApi = {}
        }
        root.TokenHistoryApi.MtContractDetail = factory(root.TokenHistoryApi.ApiClient)
    }
})(this, function(ApiClient) {
    /**
     * The MtContractDetail model module.
     * @class MtContractDetail
     * @version 1.0
     */

    /**
     * Constructs a new <code>MtContractDetail</code>.
     * @alias MtContractDetail
     * @class
     * @param address {String} Contract address (20-byte)
     * @param status {String} Labeling status (completed, processing, failed, cancelled)
     * @param createdAt {Number} Labeled (timestamp)
     * @param updatedAt {Number} Last time label information changed (timestamp)
     * @param deletedAt {Number} UNIX timestamp of when the label deleted; 0 if not deleted
     * @param type {String} Contract type (KIP-7, ERC-20)
     * @memberof MtContractDetail
     */
    const MtContractDetail = function(address, status, createdAt, updatedAt, deletedAt, type) {
        this.address = address
        this.status = status
        this.createdAt = createdAt
        this.updatedAt = updatedAt
        this.deletedAt = deletedAt
        this.type = type
    }

    /**
     * Constructs a <code>MtContractDetail</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {MtContractDetail} obj Optional instance to populate.
     * @return {MtContractDetail} The populated <code>MtContractDetail</code> instance.
     * @memberof MtContractDetail
     */
    MtContractDetail.constructFromObject = function(data, obj) {
        if (data) {
            obj = obj || new MtContractDetail()
            if (data.hasOwnProperty('address')) obj.address = ApiClient.convertToType(data.address, 'String')
            if (data.hasOwnProperty('status')) obj.status = ApiClient.convertToType(data.status, 'String')
            if (data.hasOwnProperty('createdAt')) obj.createdAt = ApiClient.convertToType(data.createdAt, 'Number')
            if (data.hasOwnProperty('updatedAt')) obj.updatedAt = ApiClient.convertToType(data.updatedAt, 'Number')
            if (data.hasOwnProperty('deletedAt')) obj.deletedAt = ApiClient.convertToType(data.deletedAt, 'Number')
            if (data.hasOwnProperty('type')) obj.type = ApiClient.convertToType(data.type, 'String')
        }
        return obj
    }

    /**
     * Contract address (20-byte)
     * @type {String}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.address = undefined

    /**
     * Labeling status (completed, processing, failed, cancelled)
     * @type {String}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.status = undefined

    /**
     * Labeled (timestamp)
     * @type {Number}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.createdAt = undefined

    /**
     * Last time label information changed (timestamp)
     * @type {Number}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.updatedAt = undefined

    /**
     * UNIX timestamp of when the label deleted; 0 if not deleted
     * @type {Number}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.deletedAt = undefined

    /**
     * Contract type (KIP-7, ERC-20)
     * @type {String}
     * @memberof MtContractDetail
     */
    MtContractDetail.prototype.type = undefined

    return MtContractDetail
})
